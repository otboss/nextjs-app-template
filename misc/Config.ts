export abstract class Config {
    public static readonly endpoint: string = "http://localhost:4444";
    public static readonly prodEndpoint: string = "";
    // The number of items to display per page
    public static readonly perPage = 5;
}